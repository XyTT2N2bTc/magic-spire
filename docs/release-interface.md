# 四区解缚界面预览

## 目标查询共享边界 · 2026-09-15

RuleChangePackage：ui/target_queries.gd承接身体／区域定位、身体装备归并、按牌筛选身体目标、单件自动选择、拖放数据解析与装备目标筛选，以及快捷解除的牌面／版本／精准目标选择。输入只有当前View中的数据、ActionIndex和本次载荷；不接收Game或控件，不写状态。main、drag_targets、quick_release_bar保留原UI入口、提示与提交；已有回调调用的薄入口转给共用查询，不保留另一份筛选算法。解除模式列表只在共享模块定义。

方法语义分别保持：身体显示按物理目标＋牌面归并，优先可用候选但保持原目标位置；通用拖牌按原手牌目标和精准槽去重，保留首项及自身目标；快捷／身体详情没有可用项时保留首个不可用原因，ActionIndex.first_usable仍保留末项。唯一装备自动选择遇捕缚等额外目标时继续要求玩家选择；显式选择的装备不可用或不存在时不改投别件。牢门开锁只随原拘束面载荷加入，火球沿原候选，旧版本继续拒绝。

状态、规则资格、费用、效果、回合、随机、事件及存档不变；changedUiAndLogs=N/A，标签、说明与具体原因保持。targeting补独立候选夹具，覆盖共享物理目标、两面牌、首／末原因、捕缚、手牌去重、牢门、火球、失效版本、指定ID顺序与返回容器隔离，并复用真实点击／拖牌案例。按受影响UI分类检查targeting、basic_attacks、body_layout、keyboard、guard、equipment_complete、casting、card_power、exploration；不重复规则全项目回归。build/target-queries-20260915只存旧UI参照与七场景9730项查询对照，完整候选、顺序、状态和View一致；结果见verification.md。

## RuleChangePackage · 2026-09-14

用户确认先做一版可查看的 UI：身体入口归为头颈、手胸、臀腿、性器；展示实际层级，执行前突出前后数值，有明显衰减时标红。此批次是界面预览，不宣告新版本完成，不打包或发布。

身体侧栏保留原立绘场景，按可用高度展开多个区域的精准部位入口，超出时收起最早开启的区域。区域点击展示该区域的装备；精准部位仍可单独检查和拖牌。同一区域同一物理目标只展示一次，覆盖位置合并；不同部位分别分组，不构造跨部位的解除先后顺序。区域件数按可见装备根去重，只作显示，不修改卡牌和遗物的计数规则。空闲部位合并为一行。

同一精准部位按真实 layer 从外到内显示，覆盖者名称来自正式外层查询；连接和附属部件单独标识。该顺序只是物理展示：挣扎仍可能要求先处理紧度最高的内层，滑脱仍按其原有外层条件判定。原候选的可用性、具体阻止原因和风险保持权威。

详情卡采用深色背景、细金边、装备小图和一列布局。卡牌确认及装备操作预览由只读 ReleaseView 从正式候选投影耐久前后值、实际减少量、紧度变化、开锁或解除结果；锁倍率、紧度倍率、目标倍率、堆叠及部位衰减以红字短提示展示。包含已投影的固定工具伤害，魔法附成功条件；限制项圈只显示开锁/取下，不把内部生命周期值显示成耐久。默认不展开公式，原完整效果仍可折叠查看，机械日志保留。

影响范围：只读装备与候选投影、身体栏、详情布局、目标高亮、相关中文及英文、测试导航和可选 UI 证据。状态、候选 ID/资格/费用、数值公式、事务、事件、固定点、回合、随机域、存档及迁移不变，十四规则交互轴不变。changedUiAndLogs：更新上述 UI；信息栏、日志、对白 N/A（没有新行动或机械变化）。继续使用候选 ID 与版本复核提交；展开、折叠和查看不推进游戏。

验证归属：equipment 覆盖零伤害滑脱、只开锁、无耐久取下及原事务；architecture 检查只读隔离；body_layout 检查四区几何、部位导航、实际层级/遮挡、数值与提交一致、取消及旧版本拒绝；equipment_complete 和 targeting 检查拖放、复合部件及具体禁用原因；display 检查场景复用；localization 检查资源。截图仅通过显式指定文件名输出到 build/，另提供只包含区域按钮和详情的截图供本次评审。执行结果见 verification.md，未执行项不记作通过。


## 自适应展开与区域严密度 · 2026-09-14

按用户补充要求，身体框330→310、区域列120→100，小部位字体12→14。多个区域按开启顺序保留；根据实际字体最小高度、行间距和当前框内高度计算总占用，新增区域放不下时依次收起最早打开的区域，恰好放下则保留。单个区域也过长时在其内部滚动。普通刷新、选择部位和关闭详情不强制收起其他区域；新开局重置展开状态。这些均为纯UI状态，不写存档或派发游戏行动。

详情页收窄20，删除重复提示，缩小图标与卡片留白，详情入口改为右侧小按钮。手胸、臀腿标题下直接显示正式restraint_degree及0—4进度，保留0.5等真实小数，不以向上取整的行动等级替代。头颈与性器没有现成的总严密度，不虚构数值，仍展示各件紧度。中文及英文同步，机械日志N/A。

验证复用body_layout：多个区域同时展开、精确高度与少1像素边界、FIFO收起、手动关闭、刷新保持、短框内部滚动、真实小数严密度和只读检查；复用targeting/equipment_complete/touch/keyboard验证原候选及输入，不做全项目发布测试。结果记录到verification.md。

卡牌确认固定在详情页底部，效果正文独立滚动，长说明不挤走打出按钮。拖牌／焦点高亮沿用原按钮内边距，避免高亮时改变小部位高度并错指下一分区；几何检查和真实拖牌均覆盖此边界。


## 战斗施加后自动展开身体区域（2026-09-14）

RuleChangePackage：正式行动成功后，以提交前后只读body_regions.targets的物理ID对比确认新增或替换的拘束具，自动展开受影响区域并更新展开顺序；普通、复合、链接及特殊装备均复用其正式区域投影。仅战斗内动作生效（包含该动作结算进入奖励／整备），不在开局、读档、SL、非战斗行动、普通重绘或失败提交时自动展开。同件纯加固／降档不触发；手动收起保持到后续真实施加。只展开左侧区域，不选择装备、不弹出详情、不派发额外动作；空间不足继续按实际高度收起旧区域，最新区域可内部滚动。

不新增游戏状态、存档、随机或事件，不改变施加资格、数量、部位、层级、费用、回合及十四规则交互轴。玩家文案N/A：使用已有区域名称、计数、展开／收起标志；无新日志或叙事。body_layout窗口补真实结束回合施加、旧版本拒绝、同数量替换、手动收起与刷新、加固反例、非战斗隔离及只读断言；复用architecture与display窗口覆盖场景刷新边界，不打包。


## 动作栏／快捷挣脱栏（2026-09-14）

横栏最右侧按钮零回合切换两页。动作页保留原体术与深呼吸；快捷页从左到右为头颈、手胸、性器、臀腿、道具使用。四个区域默认显示有可用挣脱候选的小部位，若没有则显示已佩戴或第一个部位；右键循环小部位。每格显示当前装备名、真实耐久／上限、紧度及锁状态，不可用时另显示正式原因。

按用户最终确认，先单击区域选定当前小部位和装备，再单击解除牌直接提交；支持挣扎、滑脱、魔法滑脱、降紧和开锁，魔力撑除与术式解锁也复用原候选。点击部位本身不行动、不扣费、不弹确认，也可将牌直接拖到格子。同部位多件装备以格内当前选中的物理目标为准；不自行改牌面、绕过上锁、层级、触及、施法或费用。双拘束面继续读取原候选模式。无有效目标只显示原因，不转用其他装备或区域。切回动作页后普通点牌恢复原入口。新开局清除本栏选择。

战斗、整备、休息及监狱探索保留横栏，非战斗动作页仍能使用正式深呼吸候选；休息规则提示移到上方。已安装道具入口统一进入快捷页第五格，仍打开原道具详情和候选，不占魔瓶按钮。切页局部更新横栏，保留立绘和手牌节点。拖牌高亮同时更新快捷格，取消后恢复。

RuleChangePackage：仅增加UI模式及部位ID选择，不进入GameState或存档。既有View／ActionIndex决定目标与可用性，候选ID＋版本提交不变；状态、费用、数值、事务、事件、回合、随机、迁移和十四规则交互轴均不变。新增中英文切换、操作提示及空格文案；日志／对白N/A（无新行动）。复用basic_attacks、installed_tools、exploration、targeting、body_layout窗口分类与architecture/localization规则分类；包含真实先部位后牌、真实拖放、失效／错误牌面／费用拒绝、只读切页、区域隔离、探索深呼吸及道具几何检查。未打包或发布。


## 左侧捕缚条接收拖牌（2026-09-14）

左侧资源区的捕缚整行（标签、进度条、数值）接收挣扎／滑脱等具有正式捕缚目标的牌。与战场捕缚条复用同一接收函数、当前候选和版本校验；两个入口同时高亮并显示原伤害预览，错误牌面／不支持的牌／旧版本保持拒绝，移除捕缚后同时移除接收区。原选牌后点击捕缚也可用。

RuleChangePackage：纯UI入口，原状态、目标、资格、伤害、费用、事务、回合、随机及存档／迁移和十四规则轴不变。文字复用已有捕缚说明、伤害和不可用原因（含原英文），没有新日志或对白。guard窗口分类补充实际拖到标签及条身、费用和进度一致、无效及失效松手不变、移除后接收区消失；复用targeting分类检查共享拖牌入口。未打包。


## 快捷目标循环、详情联动与探索火球术（2026-09-14）

装备名右侧显示一条48×8的剩余耐久条，只读取当前装备的正式ratio，不显示数字或百分比；空部位隐藏。名称为进度条预留空间，完整名称仍在悬浮说明中。条形不接收鼠标事件，点击和拖牌继续交给整个快捷框。原耐久／紧度文字保留，切换装备和真实行动后同步刷新。

四个快捷区域框的左右两端提供上一件／下一件三角箭头，点击区占满框高，装备信息显示在中间；先选中区域后，键盘←／→也可循环该大区的全部物理拘束具。右键继续切小部位。默认读取已有外层遮挡事实和当前可用挣扎候选，优先选择最外层且可挣扎的最低紧度目标，同档按耐久比例排列；没有这类目标时保留实际装备供查看，不伪造可用性。手动选中的物理ID优先保持，后续点击／拖牌只查这件装备的原候选，不因它不可用而暗中改到其他装备；装备消失后重新选择当前默认项。

点击快捷区域框会同步展开左侧大区、选中具体小部位，并在原装备窗口打开当前拘束具的三级详情；再点同一框关闭详情并保留已选目标，仍可直接点牌使用。点击另一框直接切换详情。对应栏位快捷键复用相同开关行为；左右箭头或右键切换始终展开新目标的详情。无装备的部位只展示原部位页面，不虚构三级装备。详情窗口在快捷模式下（含空部位）缩至横栏上方，滚动定位目标；查看不派发行动。文字、耐久、紧度、锁及原禁用原因随当前目标更新。

踢击默认键由V改为X，近身短打由X改为V；已有配置若恰好仍是旧默认组合则同步对调，其他自定义绑定保留。固定←／→只在快捷区域被选中、无模态／输入框／独立键盘目标选择／拖拽时接管，原键位设置和菜单导航继续使用。

快捷页四区复用同位置动作的当前绑定：头颈对应肘击（默认Z）、手胸对应踢击（默认X）、性器对应近身短打（默认V）、臀腿对应火球术（默认F）。按键只选择区域，选中后再用牌；格内显示真实绑定，自定义键位即时跟随。切回动作页恢复原动作选择；道具继续使用原I快捷键。此次RuleChangePackage仅改箭头几何、键盘路由与键位提示，状态、候选、费用、事务、事件、随机、存档及十四规则交互轴不变；复用原中英文区域名与箭头提示，日志和对白N/A。basic_attacks检查两侧点击区及中央文字边界，keyboard覆盖四区真实按键、自定义绑定、抽屉阻挡和切回原动作。

探索动作页固定保留火球术与深呼吸；火球术具有炫火自解效果时从原装备施法候选选取入口，单击进入已有目标选择、拖动可进入原部位目标列表或快捷格，不直接向默认装备开火。未获得自解能力或没有装备目标时保留灰色火球术，分别说明原因。原施法成功率、消耗、次数、外层和免伤资格不变，候选补齐紧凑伤害／次数文案。

RuleChangePackage：新增仅UI使用的选中物理ID及详情焦点，GameView新增火球术自解能力布尔事实；不增加游戏或存档字段、不改变规则资格／事务／事件／随机／回合及十四规则轴。新箭头、操作帮助、探索火球不可用原因同步中英文，机械日志／对白无新内容。basic_attacks补默认优先、实体目标保持、跨小部位／空部位循环、左右键、二级与三级详情联动、X/V真实输入及探索火球真实拖放；keyboard、guard、targeting、body_layout、exploration与architecture/localization按影响复核。未打包。
