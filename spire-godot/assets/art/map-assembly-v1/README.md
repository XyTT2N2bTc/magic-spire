# 地图素材拼装预览

2026-09-07。`map-preview.png` 为1600×900静态预览，直接在 HTML 中使用第一批 `map-icons-v1` 与第二批 `map-parts-v2` 的真实 PNG，并通过浏览器截图导出。没有重新生成或重绘这些图片。

`preview.html` 保存可复现的排版：纸面、两侧建筑、节点与状态叠层分别使用独立图片；路线由 SVG 曲线绘制，标题和文字使用网页字体。右侧深色区域的图例仅用显示滤镜统一成浅色，源PNG不变。

路线是用于评审素材的局部示意，非正式种子、存档或完整塔图。按钮是静态展示，未接入游戏，不改变任何规则或运行时。已打开导出的PNG检查整体排版、图片加载、透明背景和路线显示。
