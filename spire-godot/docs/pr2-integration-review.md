# PR #2 本地选择性整合 · 2026-09-17

来源：[PR #2](https://github.com/h13942080472-prog/magic-spire/pull/2)，检视头提交 `a56de58521272eb9e03a6a7102d4760c092c386e`；本地基线 `e635bf55a09b3662db85596204246e929a752ace`（v0.17）。用户要求优先架构，并明确把存档一起替换。先在 `outputs/pr2-architecture-prep` 的 `codex/pr2-architecture-prep` 工作树整合、验证，再同步本地源码；没有合并远端 PR、提交、推送或打包。

## 替换范围

| 部分 | 接入方式与边界 |
| --- | --- |
| 装备查询 | 同一次只读调用内生成部位、物理ID、覆盖点、复合根与连接索引；提交及换状态不复用旧索引。 |
| 文案查询 | CopyRouter统一分派；卡牌详情走candidate_detail，牌库／商店按需读取实例文案。保留现有UI目标查询模块及ID＋版本提交。 |
| 事件 | 12份内容包、模板、解析器、事件执行、事件快照校验、对应测试成套接入nodes/start_node与统一条件表；不能只换room_events.gd。 |
| 状态迁移 | phase/room通过_apply_transition写入，战斗结束共用判定与执行入口，保留副作用原顺序。 |
| 存档 | dispatch返回checkpoint；进更高层、战斗结束（含收押）、整备结束触发自动写盘；恢复及普通场景内提交不再写盘。新局、手动保存和地图批注仍沿原入口。磁盘继续与内存快速SL的恢复时刻明确区分；没有新增旧档迁移。 |
| 作者文档 | 新契约及更新后的三份作者文档仍放在spire-godot/docs，内容README相对链接按现有布局修正。 |

未采用PR中的根／模块指引合并、指引检查器与CI删除、文档整体搬家、release目录迁移及打包脚本路径调整；它们不属于这次架构替换，且指引删除与用户最新跨模块要求冲突。PR #3（macOS）未整合。

## 检查中补齐的问题

1. **成长牌显示回归**：同牌型仍在手牌时，已成长且处于弃牌堆的实例文案被通用牌面覆盖。`_display_card`已有实例来源时不再叠加手牌投影。牌库／弃牌堆、两面4项反例在原PR失败，v0.17基线通过，修正后通过；费用、伤害与随机未改。
2. **详情接口漏迁移**：installed_tools与hand_assist的规则／UI测试仍直接访问card候选的detail。基线这两个规则分类通过，PR却报键不存在，不能按PR说明归为基线旧失败。调用改为candidate_detail/detail_of，原伤害、费用、原因和回滚断言保留，规则44＋154、UI57＋9通过。
3. **重开时旧目标访问**：render先清理旧拖牌控件时会刷新快捷栏，按需详情由此拿上一局目标访问新游戏，报template键不存在。清理时可跳过快捷栏刷新，新View与ActionIndex建立后再正常构建；保留普通拖放取消的即时刷新。真实拖牌后重开用例及基础动作／目标分类通过。
4. **存档文字未同步**：教程和存档页改为实际三个自动保存时机，继续提示使用“最近保存的起点”；快速SL仍说明当前场景。中文、英文、生成器人工映射同步，并把PR新增的30条英文翻译补入生成来源，防止下次重建丢失。未运行离线翻译模型全量重建。

## 独立验证与限制

验证使用Windows Godot 4.7.2、tools/check.ps1、隔离测试存档；逐批源码指纹稳定。以下为受影响分类各自最新结果的合计，**不是一次全项目门禁通过**。

- 规则17分类7996项全部通过：localization 140、architecture 471、event_flow 835、content 574、persistence 955、events 877、installed_tools 44、hand_assist 154、services 513、core 888、equipment 167、links 170、composites 96、equipment_complete 453、prison 1255、guard 94、tower 310。
- UI 11分类1280项全部通过：card_growth 24、home 113、route 134、events 180、persistence 104、encyclopedia 170、display 141、targeting 112、installed_tools 57、basic_attacks 236、hand_assist 9。
- 首批遗留（随后已修复）：card_power的5条奖励归属、tower_progression的10条Boss／召唤缩放与顶层移动、home_persistence的3条新局阶段／边界布局／宝箱恢复断言。全部在**真正基线e635bf5**复现同样失败；后续定位为17条过时测试前提和1条主页布局缺陷，见下方修复结果，原失败日志保留。
- 未验证：全部分类／全部种子、Android真机、macOS、安装包、上游私有的94场景事件oracle与31场景迁移oracle。本地只报告实际运行的仓库分类，未复用PR自报的性能数据或oracle摘要作为本地通过证据。
- 上游已登记的单节点in_place事件next快照校验缺口A40保留，未作为本批已修复内容。PR还包含漂浮皮带群持有软化扣环时的条件离开选项，不能把相对v0.17的全部变化称为纯重构或玩家行为逐字节不变。


主工作区首次同步复核：102个文件逐一比对SHA256一致；`20260917T025107158-15688`在实际spire-godot工作区完成导入与architecture 471项，退出码0、源码指纹稳定。根／模块AGENTS检查通过，三份作者文档的相对链接已按保留的目录布局修正。同步清单记录的是该批版本，后续授权修复发生在主工作区。

后续18条基线失败已处理：通用／角色奖励池分开验证；周目生命预期补上夹具警戒加成；出狱重建先提交正式入口选择，再检查顶层；新局及宝箱恢复按实际阶段与控件验证，并真实点击完成房间。保存错误提示移至免责声明上方，补非空错误与缩放边界检查；没有改变玩法数值或存档结构。主工作区`20260917T025735300-30980`规则card_power 1788／tower_progression 225、UI home_persistence 51通过，同批home因左侧控件计数未同步失败；更新明确的四控件检查后，`20260917T030014632-4392`仅补跑home 113通过。两批源码指纹各自稳定，后批退出码0；最终相关规则2013项、UI164项逐批通过，详细变更包见[验证记录](verification.md)。

关键日志位于准备工作树的 `spire-godot/build/checks/`：

| 批次 | 结果 |
| --- | --- |
| 20260917T023503477-7876 | 导入及六规则分类3851项通过；本地化随后增加1项。首次空缓存导入的字体缺失发生于023414批，重导入成功，不是PR源码问题。 |
| 20260917T023651745-2396 | 成长／主页／地图／事件／存档分类通过，主页组合3条旧失败。 |
| 20260917T023944481-8680 | 图鉴／显示／目标423项通过。 |
| 20260917T024132447-13412 | 补齐旧详情接口后11规则分类4144项通过；另爬塔215项中10条旧失败。规则失败使同批UI未执行，后续单独补跑。 |
| 20260917T024543214-4560 | 基础动作／手部辅助／目标／存档通过；新重开断言错误地比较了含显示装饰的View与原候选字典，改为对照候选ID，保留引擎零错误判据。 |
| 20260917T024808356-27788 | 最终本地化140项、已安装工具UI57项通过，退出码0。 |

基线工作树 `outputs/pr2-baseline-e635bf5` 的 `023909244-4312` 批复现主页3条失败且成长牌反例23项通过；`024012792-43700` 批复现卡牌5＋爬塔10条失败，而installed_tools／hand_assist通过。正式基线代码未修改，只为成长回归附加同一UI场景。
